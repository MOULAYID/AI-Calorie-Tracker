"""sdd_reverse_scripts — CLI wrappers for the reverse engineering pipeline.

Each script is a thin orchestrator over sdd_reverse modules. Designed for
0-token determinism (called by reverse agents to delegate computation).

See .claude/docs/reverse-engineering-workflow.md §13.1 for the MVP scope.
"""
