<Window x:Class="HelloWpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connexion"
        Width="400" Height="280"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Connexion" FontSize="20" FontWeight="Bold" />

        <Label Grid.Row="1" Content="Nom d'utilisateur :" Target="{Binding ElementName=txtUsername}" />
        <TextBox Grid.Row="2" x:Name="txtUsername" Margin="0,4,0,8" />

        <Label Grid.Row="3" Content="Mot de passe :" Target="{Binding ElementName=pwdPassword}" />
        <PasswordBox Grid.Row="4" x:Name="pwdPassword" Margin="0,4,0,16" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Annuler" Width="80" Margin="0,0,8,0" Click="btnCancel_Click" />
            <Button x:Name="btnLogin" Content="Se connecter" Width="120" Click="btnLogin_Click" />
        </StackPanel>
    </Grid>
</Window>
