# .sdd/agents/reverse-sql-feat-composer.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/reverse-sql-feat-composer.md — source
# NON déplacée). model_tier dérivé de `model: claude-opus-4-8`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/reverse-sql-feat-composer.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: reverse-sql-feat-composer
description: Rung 2 LLM (OPT-IN) du reverse base de données — compose la FEAT métier d'UN module à partir
  des User Stories d'objets SQL (procédures/fonctions/vues/triggers) produites par reverse-sql-analyst.
  Synthèse métier transverse + démotion de la plomberie, à parité avec l'escalier code (3c reverse-feat-composer).
  Alternative à l'assembleur déterministe build_proc_feats.py (défaut). Lecture seule sur la base (déjà
  déconnectée) ; n'écrit que la FEAT. Aucun spawn d'agent.
model_tier: deep
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: deep
tier_floor: balanced
tier_ceiling: deep
body_source: .claude/agents/reverse-sql-feat-composer.md
