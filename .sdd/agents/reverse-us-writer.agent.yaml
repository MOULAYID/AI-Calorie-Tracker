# .sdd/agents/reverse-us-writer.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/reverse-us-writer.md — source
# NON déplacée). model_tier dérivé de `model: claude-sonnet-4-6`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/reverse-us-writer.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: reverse-us-writer
description: Barreau 3b de l'escalier reverse (ADR reverse-spec-ladder). Pour UNE unité U-N, lit l'analyse
  technique 3a (plans/{n}-{FeatName}.analysis.md) et la remonte d'altitude en User Stories par capability
  dans us/{n}-{m}-{Name}.md. Altitude moyenne (métier mais traçable). Chaque AC pointe vers les tasks
  T-N (fil de traçabilité D3). Confidence min-monotone (≤ analyse). Aucun spawn d'agent.
model_tier: balanced
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: balanced
tier_floor: fast
tier_ceiling: balanced
body_source: .claude/agents/reverse-us-writer.md
