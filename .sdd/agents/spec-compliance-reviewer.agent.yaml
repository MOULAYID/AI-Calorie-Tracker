# .sdd/agents/spec-compliance-reviewer.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/spec-compliance-reviewer.md — source
# NON déplacée). model_tier dérivé de `model: claude-sonnet-4-6`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/spec-compliance-reviewer.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: spec-compliance-reviewer
description: Agent Spec Compliance Reviewer — vérifie indépendamment que chaque Acceptance Criteria (AC)
  de chaque US est implémentée dans le code matérialisé. Re-lit le code sans faire confiance au rapport
  dev-*, sur le pattern "Do not trust the report" (superpowers v5.1). Produit `spec-compliance.{md,json}`
  avec verdict 🟢/🟡/🔴 selon `SpecComplianceFailOn`. Strictement read-only sur le code généré.
model_tier: balanced
tools:
- Read
- Write
- Glob
- Grep
- Bash
tier_default: balanced
tier_floor: balanced
tier_ceiling: deep
body_source: .claude/agents/spec-compliance-reviewer.md
