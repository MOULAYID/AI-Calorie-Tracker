# .sdd/agents/reverse-clarifier.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/reverse-clarifier.md — source
# NON déplacée). model_tier dérivé de `model: claude-sonnet-4-6`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/reverse-clarifier.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: reverse-clarifier
description: Boucle de validation humaine structurée (Phase 3.9, optionnel). Mode generate — consolide
  les gaps du reverse (complétude, traçabilité, items medium/low) en questions structurées dans workspace/old/{P}/.sys/questions.md,
  que le Tech Lead remplit. Mode --ingest — ré-injecte les réponses dans les FEATs concernées (marqueur
  human-validated, confidence recalculée, REVERSE-GATE resynchronisée, hash US re-résolu) puis re-valide.
  Jamais d'invention de réponse. Aucun spawn d'agent.
model_tier: balanced
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: balanced
tier_floor: fast
tier_ceiling: balanced
body_source: .claude/agents/reverse-clarifier.md
