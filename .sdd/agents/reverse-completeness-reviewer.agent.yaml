# .sdd/agents/reverse-completeness-reviewer.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/reverse-completeness-reviewer.md — source
# NON déplacée). model_tier dérivé de `model: claude-sonnet-4-6`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/reverse-completeness-reviewer.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: reverse-completeness-reviewer
description: 'Reviewer "back" du workflow reverse (L5). Pour UNE FEAT reverse, confronte son contenu à
  l''evidence profonde de l''unité (inventory units[U-N].classes + dataAccess : repositories, services,
  requêtes SQL, procédures stockées) et signale ce que l''extraction a OMIS. Verdict informational (jamais
  bloquant), miroir reverse de spec-compliance-reviewer. Délègue le calcul au script déterministe check_feat_completeness.py.
  Aucun spawn d''agent.'
model_tier: balanced
tools:
- Read
- Write
- Glob
- Grep
- Bash
tier_default: balanced
tier_floor: balanced
tier_ceiling: balanced
body_source: .claude/agents/reverse-completeness-reviewer.md
