# .sdd/agents/elicitor.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/elicitor.md — source
# NON déplacée). model_tier dérivé de `model: claude-sonnet-4-6`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/elicitor.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: elicitor
description: Agent Élicitation — enrichit une FEAT fonctionnelle via une bibliothèque de 15 techniques
  d'élicitation (Pre-mortem, First Principles, Red Team, Stakeholder Mapping, Inversion, SCAMPER, Reverse
  Brainstorming, 5 Whys, Customer Journey Mapping, Empathy Map, Crazy 8s, Six Thinking Hats, Cynefin,
  OKR Decomposition, Lotus Blossom). Détecte le contexte FEAT et recommande 2-3 techniques adaptées. Produit
  des sections enrichies en fin de FEAT + met à jour la constitution §7. Mode interactif (questions ciblées)
  ou one-shot (--quick).
model_tier: balanced
tools:
- Read
- Write
- Edit
- Glob
- Grep
- AskUserQuestion
- Bash
tier_default: balanced
tier_floor: balanced
tier_ceiling: balanced
body_source: .claude/agents/elicitor.md
