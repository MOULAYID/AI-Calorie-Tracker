# .sdd/agents/constitutioner.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/constitutioner.md — source
# NON déplacée). model_tier dérivé de `model: claude-sonnet-4-6`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/constitutioner.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: constitutioner
description: Agent Constitutioner — gère les ADRs (création atomique par timestamp) et met à jour workspace/.sys/.context/constitution.md
  (§4 stack technique, §6 ADRs index, §1 date). Invoqué par arch en fin de Phase B (après scaffolding
  DB). Skip silencieusement si constitution.md absent. Aucune écriture de code applicatif, aucune lecture
  des FEATs/US/HTML.
model_tier: balanced
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: balanced
tier_floor: fast
tier_ceiling: balanced
body_source: .claude/agents/constitutioner.md
