# .sdd/agents/qa.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/qa.md — source
# NON déplacée). model_tier dérivé de `model: claude-sonnet-4-6`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/qa.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: qa
description: 'Agent QA — génère tests unitaires (backend + frontend) à partir des US et du code généré,
  parse la coverage, exécute le quality scan (sonar-like). Strict scope test : ne modifie JAMAIS le code
  de production. Token-efficient (Sonnet 4.6 + scripts déterministes pour coverage et quality).'
model_tier: balanced
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: balanced
tier_floor: balanced
tier_ceiling: deep
body_source: .claude/agents/qa.md
