# .sdd/agents/reverse-feat-composer.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/reverse-feat-composer.md — source
# NON déplacée). model_tier dérivé de `model: claude-opus-4-8`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/reverse-feat-composer.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: reverse-feat-composer
description: Barreau 3c (haut) de l'escalier reverse (ADR reverse-spec-ladder). Pour UNE unité U-N, lit
  les US 3b (us/{n}-{m}-{Name}.md) + l'analyse 3a et compose la FEAT métier propre (feats/{n}-{FeatName}.md),
  plomberie démotée, evidence file:line résolue transitivement (FEAT→US→T-N), REVERSE-GATE + confidence
  min-monotone. Itère validate_reverse_feat (max 3) + check_feat_completeness. Remplace l'ex-reverse-functional-extractor.
  Aucun spawn d'agent.
model_tier: deep
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: deep
tier_floor: balanced
tier_ceiling: deep
body_source: .claude/agents/reverse-feat-composer.md
