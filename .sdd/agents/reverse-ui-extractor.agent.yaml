# .sdd/agents/reverse-ui-extractor.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/reverse-ui-extractor.md — source
# NON déplacée). model_tier dérivé de `model: claude-opus-4-8`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/reverse-ui-extractor.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: reverse-ui-extractor
description: Pour UNE unité U-N donnée (Phase 4 reverse), lit les templates legacy + CSS + le FEAT déjà
  produit par Phase 3, et synthétise 1 à N écrans HTML sémantiques préservant la structure visuelle legacy
  SANS clonage pixel-perfect. Délègue parsing à css_palette_extractor + ui_template_parser. Output workspace/ui/{n}-{m}-{Name}.html.
  Aucun spawn d'agent.
model_tier: deep
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: deep
tier_floor: balanced
tier_ceiling: deep
body_source: .claude/agents/reverse-ui-extractor.md
