# .sdd/agents/reverse-tech-analyst.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/reverse-tech-analyst.md — source
# NON déplacée). model_tier dérivé de `model: claude-opus-4-8`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/reverse-tech-analyst.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: reverse-tech-analyst
description: Barreau 3a de l'escalier reverse (ADR reverse-spec-ladder). Pour UNE unité U-N, lit l'evidence
  + DB schema + tech-audit optionnel et produit une ANALYSE TECHNIQUE legacy (comportements observés,
  accès données, calculs, effets de bord) dans plans/{n}-{Name}.analysis.md. Photo fidèle du code, evidence
  file:line par task, confidence cap par langage, bias toward present. NE produit PAS de FEAT (c'est 3c).
  Aucun spawn d'agent.
model_tier: deep
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: deep
tier_floor: balanced
tier_ceiling: deep
body_source: .claude/agents/reverse-tech-analyst.md
