# .sdd/agents/reverse-tech-auditor.agent.yaml — pivot neutre Phase 1 (ADDITIF, généré
# 2026-07-24 depuis le frontmatter de .claude/agents/reverse-tech-auditor.md — source
# NON déplacée). model_tier dérivé de `model: claude-sonnet-4-6`
# (opus→deep, sonnet→balanced). Bornes reprises de .sdd/agent-bounds.yaml.
# NOTE body_source : le corps markdown de l'agent N'EST PAS recopié ici —
# il reste dans .claude/agents/reverse-tech-auditor.md et sera migré/transpilé en Phase 2
# (harness_build.py). Ce pivot est réversible (suppression sans impact).
schema: sdd.agent/v1
name: reverse-tech-auditor
description: Audit architecture/anti-patterns/dépendances EOL d'un projet legacy déjà inventorié (Phase
  2). Lit inventory.json + db-schema.json + deps-graph.json + selective fichiers entry-point. Produit
  tech-audit.md (FR narratif) + enrichit db-schema.enrichment.json (FICHIER SÉPARÉ, jamais db-schema.json
  — ADV-3). Output informational, non consommé par /sdd-full. Aucun spawn d'agent.
model_tier: balanced
tools:
- Read
- Write
- Edit
- Glob
- Grep
- Bash
tier_default: balanced
tier_floor: balanced
tier_ceiling: deep
body_source: .claude/agents/reverse-tech-auditor.md
