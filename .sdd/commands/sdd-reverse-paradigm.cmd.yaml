# .sdd/commands/sdd-reverse-paradigm.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-paradigm.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-paradigm.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-paradigm
has_frontmatter: true
description: Analyse de gap de paradigme legacy↔stack cible + curation des unités (MIGRATE / DISCARD / HUMAN-DECISION). Spawn agent reverse-paradigm-advisor (Sonnet 4.6). Verdict informational/WARN, jamais bloquant, jamais destructif. Emprunt Reversa (agents Paradigm Advisor + Curator) — audit comparatif 2026-06-12.
frontmatter:
  command: sdd-reverse-paradigm
  phase: '"2.7"'
  description: Analyse de gap de paradigme legacy↔stack cible + curation des unités (MIGRATE / DISCARD / HUMAN-DECISION). Spawn agent reverse-paradigm-advisor (Sonnet 4.6). Verdict informational/WARN, jamais bloquant, jamais destructif. Emprunt Reversa (agents Paradigm Advisor + Curator) — audit comparatif 2026-06-12.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-paradigm.md
