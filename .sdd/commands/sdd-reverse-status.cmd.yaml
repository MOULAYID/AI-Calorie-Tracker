# .sdd/commands/sdd-reverse-status.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-status.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-status.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-status
has_frontmatter: true
description: Diagnostic du workflow reverse engineering (pendant de /sdd-status). Liste les projets legacy sous workspace/old/ avec leur phase status, et les FEATs reverse dans workspace/feats/ avec marker [REV]/[REV⚠️] (ADV-6). Read-only, jamais bloquant. Script déterministe, aucun agent spawné.
frontmatter:
  command: sdd-reverse-status
  phase: diagnostic
  description: Diagnostic du workflow reverse engineering (pendant de /sdd-status). Liste les projets legacy sous workspace/old/ avec leur phase status, et les FEATs reverse dans workspace/feats/ avec marker [REV]/[REV⚠️] (ADV-6). Read-only, jamais bloquant. Script déterministe, aucun agent spawné.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-status.md
