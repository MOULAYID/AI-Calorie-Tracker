# .sdd/commands/sdd-reverse-init.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-init.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-init.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-init
has_frontmatter: true
description: Bootstrap workspace/old/{LegacyProject}/.sys/ pour le workflow reverse engineering. Crée le squelette de dossiers + un README minimal. Ne touche pas au framework. Read-only sur le legacy.
frontmatter:
  command: sdd-reverse-init
  phase: '0'
  description: Bootstrap workspace/old/{LegacyProject}/.sys/ pour le workflow reverse engineering. Crée le squelette de dossiers + un README minimal. Ne touche pas au framework. Read-only sur le legacy.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-init.md
