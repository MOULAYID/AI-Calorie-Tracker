# .sdd/commands/sdd-reverse-crosscut.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-crosscut.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-crosscut.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-crosscut
has_frontmatter: true
description: Phase 3-bis du workflow reverse — génère les FEATs transversales déterministes (Librairies à installer + Base de données/procédures stockées/connection strings) depuis les artefacts L1. Script pur (0 token, aucun agent spawné).
frontmatter:
  command: sdd-reverse-crosscut
  phase: '"3-bis"'
  description: Phase 3-bis du workflow reverse — génère les FEATs transversales déterministes (Librairies à installer + Base de données/procédures stockées/connection strings) depuis les artefacts L1. Script pur (0 token, aucun agent spawné).
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-crosscut.md
