# .sdd/commands/sdd-reverse-audit.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-audit.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-audit.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-audit
has_frontmatter: true
description: Phase 2 du workflow reverse — audit informational. Spawn reverse-tech-auditor (Sonnet 4.6) après exécution du script déterministe reverse_audit.py (deps-graph + merge db-schema). Output non bloquant pour Phase 3.
frontmatter:
  command: sdd-reverse-audit
  phase: '2'
  description: Phase 2 du workflow reverse — audit informational. Spawn reverse-tech-auditor (Sonnet 4.6) après exécution du script déterministe reverse_audit.py (deps-graph + merge db-schema). Output non bloquant pour Phase 3.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-audit.md
