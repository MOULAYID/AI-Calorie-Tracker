# .sdd/commands/sdd-proc-reverse-full.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-proc-reverse-full.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-proc-reverse-full.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-proc-reverse-full
has_frontmatter: true
description: Reverse engineering de TOUS les objets SQL exécutables d'une base (lecture seule) — procédures stockées, fonctions, vues et triggers (P0.1 2026-07-24). Introspecte via la connection string de stack.md (## Active Database), regroupe les objets en modules, génère 1 User Story par objet SQL et 1 FEAT par module. Multi-dialecte (SQL Server complet ; PostgreSQL procs/fonctions). Ne modifie JAMAIS la base.
frontmatter:
  command: sdd-proc-reverse-full
  phase: proc-reverse
  description: Reverse engineering de TOUS les objets SQL exécutables d'une base (lecture seule) — procédures stockées, fonctions, vues et triggers (P0.1 2026-07-24). Introspecte via la connection string de stack.md (## Active Database), regroupe les objets en modules, génère 1 User Story par objet SQL et 1 FEAT par module. Multi-dialecte (SQL Server complet ; PostgreSQL procs/fonctions). Ne modifie JAMAIS la base.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-proc-reverse-full.md
