# .sdd/commands/sdd-reverse-ui.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-ui.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-ui.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-ui
has_frontmatter: true
description: Phase 4 du workflow reverse — extraction UI sémantique d'UNE unité U-N en mockup(s) HTML statique. Spawn agent reverse-ui-extractor (Opus 4.8). Output workspace/ui/{n}-{m}-{Name}.html consommable par dev-frontend lors de /sdd-full.
frontmatter:
  command: sdd-reverse-ui
  phase: '4'
  description: Phase 4 du workflow reverse — extraction UI sémantique d'UNE unité U-N en mockup(s) HTML statique. Spawn agent reverse-ui-extractor (Opus 4.8). Output workspace/ui/{n}-{m}-{Name}.html consommable par dev-frontend lors de /sdd-full.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-ui.md
