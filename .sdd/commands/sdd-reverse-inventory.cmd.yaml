# .sdd/commands/sdd-reverse-inventory.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-inventory.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-inventory.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-inventory
has_frontmatter: true
description: Phase 1 du workflow reverse — cartographie déterministe d'un projet legacy. Spawn agent reverse-inventory (Sonnet 4.6) qui délègue au script reverse_inventory.py + enrichit inventory.md. Output bloquant pour Phase 3 (/sdd-reverse {U-N}).
frontmatter:
  command: sdd-reverse-inventory
  phase: '1'
  description: Phase 1 du workflow reverse — cartographie déterministe d'un projet legacy. Spawn agent reverse-inventory (Sonnet 4.6) qui délègue au script reverse_inventory.py + enrichit inventory.md. Output bloquant pour Phase 3 (/sdd-reverse {U-N}).
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-inventory.md
