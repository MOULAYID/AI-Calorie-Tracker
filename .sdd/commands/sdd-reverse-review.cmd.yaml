# .sdd/commands/sdd-reverse-review.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-review.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-review.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-review
has_frontmatter: true
description: Revue de complétude back (L5) d'UNE FEAT reverse. Spawn agent reverse-completeness-reviewer (Sonnet 4.6) après le check déterministe check_feat_completeness.py. Verdict informational, jamais bloquant. Commande wrapper créée 2026-06-10 (audit M11) pour que /sdd-reverse-full reste un pur séquenceur de commandes (no-spawn §9).
frontmatter:
  command: sdd-reverse-review
  phase: '"3.6"'
  description: Revue de complétude back (L5) d'UNE FEAT reverse. Spawn agent reverse-completeness-reviewer (Sonnet 4.6) après le check déterministe check_feat_completeness.py. Verdict informational, jamais bloquant. Commande wrapper créée 2026-06-10 (audit M11) pour que /sdd-reverse-full reste un pur séquenceur de commandes (no-spawn §9).
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-review.md
