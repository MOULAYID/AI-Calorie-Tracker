# .sdd/commands/sdd-reverse-parity.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-parity.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-parity.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-parity
has_frontmatter: true
description: Génération des specs de parité comportementale Gherkin (.feature) pour UNE FEAT reverse. Spawn agent reverse-parity-inspector (Sonnet 4.6), validation déterministe validate_parity_features.py. Verdict informational, jamais bloquant. Emprunt Reversa (agent Inspector) — audit comparatif 2026-06-12.
frontmatter:
  command: sdd-reverse-parity
  phase: '"3.8"'
  description: Génération des specs de parité comportementale Gherkin (.feature) pour UNE FEAT reverse. Spawn agent reverse-parity-inspector (Sonnet 4.6), validation déterministe validate_parity_features.py. Verdict informational, jamais bloquant. Emprunt Reversa (agent Inspector) — audit comparatif 2026-06-12.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-parity.md
