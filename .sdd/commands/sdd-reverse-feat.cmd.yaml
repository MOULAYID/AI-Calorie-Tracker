# .sdd/commands/sdd-reverse-feat.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-feat.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-feat.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-feat
has_frontmatter: true
description: Phase 3c du workflow reverse — composition de la FEAT métier propre à partir des User Stories 3b (barreau haut de l'escalier). Spawn agent reverse-feat-composer avec modèle routé par complexité (Sonnet 4.6 si unité simple, Opus 4.8 si complexe — ADR governance-reverse-complexity-ladder). Lit us/{n}-{m}-{Name}.md + plans/{n}-{Name}.analysis.md, écrit feats/{n}-{Name}.md consommable par /sdd-full.
frontmatter:
  command: sdd-reverse-feat
  phase: 3c
  description: Phase 3c du workflow reverse — composition de la FEAT métier propre à partir des User Stories 3b (barreau haut de l'escalier). Spawn agent reverse-feat-composer avec modèle routé par complexité (Sonnet 4.6 si unité simple, Opus 4.8 si complexe — ADR governance-reverse-complexity-ladder). Lit us/{n}-{m}-{Name}.md + plans/{n}-{Name}.analysis.md, écrit feats/{n}-{Name}.md consommable par /sdd-full.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-feat.md
