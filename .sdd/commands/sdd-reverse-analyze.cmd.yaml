# .sdd/commands/sdd-reverse-analyze.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-reverse-analyze.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-reverse-analyze.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-reverse-analyze
has_frontmatter: true
description: Phase 3a du workflow reverse — analyse technique fidèle d'UNE unité U-N (barreau bas de l'escalier reverse). Spawn agent reverse-tech-analyst avec modèle routé par complexité (Sonnet 4.6 si unité simple, Opus 4.8 si complexe — ADR governance-reverse-complexity-ladder). Output workspace/plans/{n}-{Name}.analysis.md, consommé par /sdd-reverse-stories (3b).
frontmatter:
  command: sdd-reverse-analyze
  phase: 3a
  description: Phase 3a du workflow reverse — analyse technique fidèle d'UNE unité U-N (barreau bas de l'escalier reverse). Spawn agent reverse-tech-analyst avec modèle routé par complexité (Sonnet 4.6 si unité simple, Opus 4.8 si complexe — ADR governance-reverse-complexity-ladder). Output workspace/plans/{n}-{Name}.analysis.md, consommé par /sdd-reverse-stories (3b).
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-reverse-analyze.md
