# .sdd/commands/arch-init.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/arch-init.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/arch-init.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: arch-init
has_frontmatter: false
description: /arch-init — Bootstrap idempotent (projets vides + scaffolding DB)
body_source: .claude/commands/arch-init.md
