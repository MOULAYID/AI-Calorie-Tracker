# .sdd/commands/sdd-proc-reverse.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-proc-reverse.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-proc-reverse.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-proc-reverse
has_frontmatter: true
description: Reverse engineering d'UN objet SQL exécutable — procédure stockée, fonction, vue ou trigger (P0.1 2026-07-24) — en lecture seule via la connection string de stack.md. Introspecte l'objet, génère sa User Story (1 objet SQL = 1 US) et met à jour/crée la FEAT du module métier auquel il appartient. Multi-dialecte (SQL Server complet ; PostgreSQL procs/fonctions). Ne modifie JAMAIS la base.
frontmatter:
  command: sdd-proc-reverse
  phase: proc-reverse
  description: Reverse engineering d'UN objet SQL exécutable — procédure stockée, fonction, vue ou trigger (P0.1 2026-07-24) — en lecture seule via la connection string de stack.md. Introspecte l'objet, génère sa User Story (1 objet SQL = 1 US) et met à jour/crée la FEAT du module métier auquel il appartient. Multi-dialecte (SQL Server complet ; PostgreSQL procs/fonctions). Ne modifie JAMAIS la base.
  loader: .claude/loader.reverse.yml
body_source: .claude/commands/sdd-proc-reverse.md
