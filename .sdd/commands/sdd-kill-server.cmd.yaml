# .sdd/commands/sdd-kill-server.cmd.yaml — pivot neutre couche COMMANDES (ADDITIF,
# généré 2026-07-24 depuis .claude/commands/sdd-kill-server.md — source NON déplacée,
# lecture seule). NOTE body_source : le corps markdown N'EST PAS recopié ici —
# il reste dans .claude/commands/sdd-kill-server.md et est réattaché par
# harness_build.py (ClaudeAdapter.emit_commands). Les valeurs `frontmatter:`
# sont RAW (guillemets préservés) pour un round-trip exact.
# Pivot réversible (suppression sans impact sur le vivant).
schema: sdd.command/v1
name: sdd-kill-server
has_frontmatter: false
description: /sdd-kill-server — Arrête backend + frontend + console SDD
body_source: .claude/commands/sdd-kill-server.md
